var str = "Không phải là 'không' muốn không";
var str2 = "không";

// alert(str.indexOf(str2)); // tìm kiếm

//alert(str.substring(1,5)); // cắt chuỗi

var str_arr = str.split("không");
alert(str_arr[02]);